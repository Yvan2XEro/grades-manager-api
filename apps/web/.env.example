VITE_SERVER_URL=
# Default organization slug to use when not running on a subdomain (e.g., localhost)
# This slug must match an organization in Better Auth
VITE_DEFAULT_ORGANIZATION_SLUG=inses-institution
